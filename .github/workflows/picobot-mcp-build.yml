name: '[RETIRED] Picobot MCP Build'
# This workflow completed its mission: MCP support shipped in official
# picobot releases (v0.2.0, 2026-03-18). picobot-update.yml handles
# ongoing release tracking. Retired 2026-03-25.
on:
  workflow_dispatch:
    inputs:
      note:
        description: 'This workflow is retired. picobot-update.yml handles updates now.'
        default: 'retired'
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This workflow is retired. MCP support landed in official picobot v0.2.0. picobot-update.yml now handles all updates.'
