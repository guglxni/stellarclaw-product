name: '[RETIRED] Picobot Auto-Update'
# Replaced by picobot-ci.yml — unified pipeline that always applies
# LiveClaw patches before deploying. This workflow deployed UNPATCHED
# binaries which overwrote file handling patches every 6 hours.
# Retired 2026-04-14.
on:
  workflow_dispatch:
    inputs:
      note:
        description: 'This workflow is retired. Use picobot-ci.yml instead.'
        default: 'retired'
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This workflow is retired. picobot-ci.yml now handles all picobot builds and deploys with patches applied.'
