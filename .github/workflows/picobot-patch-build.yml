name: '[RETIRED] Picobot Patched Build'
# Replaced by picobot-ci.yml — unified pipeline that handles both
# upstream release detection AND patch application in one workflow.
# Retired 2026-04-14.
on:
  workflow_dispatch:
    inputs:
      note:
        description: 'This workflow is retired. Use picobot-ci.yml instead.'
        default: 'retired'
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This workflow is retired. picobot-ci.yml now handles all picobot builds and deploys.'
