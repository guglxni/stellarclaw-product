// LiveClaw Frontend Configuration
// AUTO-GENERATED by deploy-frontend.sh — DO NOT EDIT MANUALLY
// To change these values, update liveclaw-web/.env on the frontend droplet
// and re-run: ./scripts/deploy-frontend.sh --config-only
(function () {
  'use strict';
  window.LIVECLAW_API_BASE              = '{{LIVECLAW_API_BASE}}';
  window.LIVECLAW_GOOGLE_CLIENT_ID       = '{{LIVECLAW_GOOGLE_CLIENT_ID}}';
  window.LIVECLAW_TURNSTILE_SITE_KEY     = '{{LIVECLAW_TURNSTILE_SITE_KEY}}';
  // No frontend key needed for Dodo Payments — checkout is server-side.
  // DEFERRED: window.LIVECLAW_APPLIXIR_ZONE_ID = '{{LIVECLAW_APPLIXIR_ZONE_ID}}';

  // ── Analytics (all optional — leave blank to disable) ──────────────────────
  // PostHog  — FOSS product analytics (github.com/PostHog/posthog)
  window.LIVECLAW_POSTHOG_KEY            = '{{LIVECLAW_POSTHOG_KEY}}';
  window.LIVECLAW_POSTHOG_HOST           = '{{LIVECLAW_POSTHOG_HOST}}';
  // Umami    — FOSS page analytics (github.com/umami-software/umami)
  window.LIVECLAW_UMAMI_URL              = '{{LIVECLAW_UMAMI_URL}}';
  window.LIVECLAW_UMAMI_WEBSITE_ID       = '{{LIVECLAW_UMAMI_WEBSITE_ID}}';
  // GTM      — tag container for future ad pixels (Meta, Google Ads, TikTok…)
  window.LIVECLAW_GTM_ID                 = '{{LIVECLAW_GTM_ID}}';
})();
